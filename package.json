{
  "name": "sky-code",
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:cli": "bun run --watch packages/cli/src/index.tsx",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@types/react": "^19.2.18",
    "prettier": "^3.8.3"
  },
  "dependencies": {
    "zustand": "4.5.2"
  }
}
